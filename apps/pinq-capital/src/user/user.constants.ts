// constants.ts

export enum Status {
  INACTIVE = 0,
  ACTIVE = 1,
  // Add other status values as needed
}

export const DEFAULT_STATUS = Status.INACTIVE;

// Add other constants as needed

// Role Constants
export enum Role {
  ADMIN = "admin",
  USER = "user",
  // Add other role values as needed
}
